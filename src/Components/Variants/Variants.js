import React from 'react';
import {useSelector} from "react-redux";

const Variants = (  question, categorycorrect, incorrect ) => {
    return (
        <div>

        </div>
    );
};

export default Variants;